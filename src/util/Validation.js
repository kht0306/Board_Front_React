export const validation = (data) => {
  let isValid = false;
  // const { title, subTitle } = data;
  // if (title && title.length > 100) {
  //     isValid = false
  // } else if (subTitle && ) {
  //     isValid = false
  // }

  return isValid;
};
